#include <iostream>
#include "Math.h"
using namespace std;
int main() 
    Math math;
    std::Math.Add(11, 78) <<' '<< Math.Add( 4,5,6)<<' '<<Math.Add(5.3, 22.72)<<' '<<Math.Add(1.5, 4.3, 3.4) << '\n';
    std::cout << math.Mul(11, 78) << ' ' << math.Mul(4, 5, 6) << ' ' << math.Mul(2.4, 4.2) << ' ' << math.Mul(1.5, 5.3, 22.72) << '\n';
    std::cout << math.Add(3, 44, 67, 45) << '\n';
    cout << math.Add("unu", "doi");
    return 0;
}